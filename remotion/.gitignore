node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Local-only stinger reference media (mov/wav) — keep the folder, ignore contents.
reference/*
!reference/.gitkeep
